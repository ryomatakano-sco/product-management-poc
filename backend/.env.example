DATABASE_URL=mysql+aiomysql://appuser:apppass@db:3306/product_management
DB_PORT=3306
DB_USER=root
DB_PASSWORD=admin
DB_NAME=maindb
OPENAI_API_KEY=sk-your-key-here

# Dev panel (bottom-left </> menu): every /dev/* call must send this as
# the X-Dev-Password header. Leave unset to disable the password.
# DEV_PANEL_PASSWORD=sco-dev
